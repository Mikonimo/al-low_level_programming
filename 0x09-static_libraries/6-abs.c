#include "main.h"
/**
  * _abs - calculate the absolute value of an integer
  * @i: thre value being computed
  * Return: 0 success
  */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
