#include <stdio.h>
/**
 * main -entry point
 * @argc: the count of arguments
 * @argv: the pointer to the string arguments
 *
 * Return: 0 on success
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
