#include <stdio.h>
/**
 * main - enty point
 * @argc: no of arguments
 * @argv: array of stringd=s
 *
 * Return: 0 success, otherwise failure
 */
int main(int argc, char *argv[])
{
	if (argc < 0)
	{
	}
	printf("%d\n", argc - 1);
	return (0);
}
